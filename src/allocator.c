#include "allocator.h"

void* reserver(size_t size){
    char file_name[] = "allocation.txt";
    size_t file_size = 4096;
    int fd = open()
    void* ptr = mmap(NULL, )
}

int main(){
    return 0;
}
